meta {
  name: Status
  type: http
  seq: 1
}

get {
  url: {{URL}}/status
  body: none
  auth: none
}

headers {
  token: {{esp_token}}
}

assert {
  res.status: 200
}

script:pre-request {
  bru.setVar("esp_token", bru.getProcessEnv("ESP_TOKEN"));
}
