meta {
  name: Areas Search -Text-
  type: http
  seq: 4
}

get {
  url: {{URL}}/areas_search?text=fourways
  body: none
  auth: none
}

query {
  text: fourways
}

headers {
  token: {{esp_token}}
}

assert {
  res.status: 200
}

script:pre-request {
  bru.setVar("esp_token", bru.getProcessEnv("ESP_TOKEN"));
}
